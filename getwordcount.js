//Create a function that takes a string and returns the word count.
//Assumption:The string will be a sentence.

function countWords(str) {
	return str.split(" ").length;
}
//returns the number of words in a string
